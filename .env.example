# =============================================================================
# shelfr Environment Configuration
# Copy to config/.env and fill in your values
#
# Environment variables are loaded using pydantic-settings for type-safe
# validation. See src/shelfr/env_settings.py for the full list.
#
# Precedence: config.yaml environment section > .env file > defaults
# =============================================================================

# -----------------------------------------------------------------------------
# Docker / Libation
# These settings control Docker container access for Libation integration
# -----------------------------------------------------------------------------
# Libation Docker container name
LIBATION_CONTAINER=libation

# Docker binary path (ensure it's executable)
DOCKER_BIN=/usr/bin/docker

# -----------------------------------------------------------------------------
# qBittorrent (REQUIRED for MAM uploads)
# These credentials are required for uploading torrents to qBittorrent
# -----------------------------------------------------------------------------
# qBittorrent Web UI URL (must include http:// or https://)
QB_HOST=http://10.1.60.10:8080

# qBittorrent Web UI username
QB_USERNAME=admin

# qBittorrent Web UI password
QB_PASSWORD=changeme

# -----------------------------------------------------------------------------
# Unraid File Ownership (for permission fixing)
# Default: nobody:users (99:100) - standard Unraid ownership
# -----------------------------------------------------------------------------
TARGET_UID=99
TARGET_GID=100

# -----------------------------------------------------------------------------
# Environment / Logging
# Application-level settings
# -----------------------------------------------------------------------------
# Environment name (development | production)
SHELFR_ENV=production

# Logging level (DEBUG | INFO | WARNING | ERROR | CRITICAL)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Audiobookshelf (optional - for ABS import integration)
# Required only if using abs-import command
# -----------------------------------------------------------------------------
# ABS server URL (must include http:// or https://, no trailing slash)
# Use HTTPS for secure token transmission
AUDIOBOOKSHELF_HOST=https://abs.example.com

# API token (Settings → Users → Your User → API Token)
AUDIOBOOKSHELF_API_KEY=your-api-token-here

# -----------------------------------------------------------------------------
# Path Overrides (optional)
# Override default XDG-compliant paths from platformdirs
# Useful for Unraid or custom deployments
# -----------------------------------------------------------------------------
# Override data directory (default: ~/.local/share/shelfr on Linux)
# SHELFR_DATA_DIR=/mnt/cache/appdata/shelfr/data
# Legacy: Shelfr_DATA_DIR also supported for backward compatibility

# Override cache directory (default: ~/.cache/shelfr on Linux)
# SHELFR_CACHE_DIR=/mnt/cache/appdata/shelfr/cache
# Legacy: Shelfr_CACHE_DIR also supported for backward compatibility

# Override log directory (default: ~/.local/state/shelfr on Linux)
# SHELFR_LOG_DIR=/mnt/cache/appdata/shelfr/logs
# Legacy: Shelfr_LOG_DIR also supported for backward compatibility
